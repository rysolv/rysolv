import React from 'react';

import { EmptyComponentContainer } from './styledComponents';

const EmptyComponent = () => (
  <EmptyComponentContainer>No issues could be found.</EmptyComponentContainer>
);

export default EmptyComponent;
