import React from 'react';

import { ComponentText } from '../styledComponents';

const InvoicesComponent = () => (
  <div>
    <ComponentText>
      View list of previous invoices. You can also click on them to view as a
      PDF document.
    </ComponentText>
  </div>
);

export default InvoicesComponent;
