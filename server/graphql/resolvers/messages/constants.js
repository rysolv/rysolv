const createMessageError = `Something went wrong when sending your message.`;

const createMessageSuccess = `The candidate has successfully been notified.`;

const getMessagesError = `Something went wrong loading messages.`;

const readMessageError = `Something went wrong marking message as read.`;

const readMessageSuccess = `Message marked as read.`;

module.exports = {
  createMessageError,
  createMessageSuccess,
  getMessagesError,
  readMessageError,
  readMessageSuccess,
};
