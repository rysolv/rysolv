const createMessage = require('./createMessage');
const getMessages = require('./getMessages');
const setReadMessage = require('./setReadMessage');

module.exports = {
  createMessage,
  getMessages,
  setReadMessage,
};
